//数组与链表区别
  1.访问成本
    数组：时间复杂度O(1)
    链表：需遍历，时间复杂度O(n)
  2.内存需求
    数组：需预留一整块内存空间，可能用不完；一旦数组被填满，需求额外空间时，将更花时间
    链表：有多少元素就用多少空间
  3.增删改插数据的复杂度
    分情况，在头部插入数据；在结尾插入数据；在中间插入数据，时间复杂度分别为
    数组：O(n);O(1)/O(n);O(n)
    链表：O(1);O(n);O(n)
